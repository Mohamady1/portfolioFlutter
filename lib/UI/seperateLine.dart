// ignore: file_names
import 'package:flutter/material.dart';

class Seperate extends StatelessWidget {
  const Seperate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      height: 30,
      width: 2,
    );
  }
}
